# 2026-03-11

- Reviewed the new three-body-council autoimprover rollout across:
  - `agents/clawdia/skills/three-body-council`
  - `agents/clawdia/runtime/intent_classifier.js`
  - `agents/clawdia/runtime/helpdesk_orchestrator.js`
- Found and fixed classifier parsing bug: string `"false"` hints from model output were being coerced to `true` by `Boolean("false")`.
- Added regression coverage in `scripts/validate-three-body-council.js` for string boolean hints.
- Fixed skill frontmatter for validator compatibility by moving `homepage` under `metadata`.
- Added release preflight script:
  - `scripts/preflight-three-body-council-release.sh`
  - Runs runtime validations, Python compile check, and skill schema validation.
- Added ClawHub release handoff doc:
  - `docs/releases/three-body-council-clawhub.md`
- Confirmed local validations pass through the preflight script.
- Implemented college essay skill release validation tooling and preflight checks.
- Security hardening pass completed:
  - Added path traversal guards for dossier IDs in `agents/clawdia/runtime/dossier_helpers.js`.
  - Added traversal regression tests in `scripts/validate-helpdesk-runtime.js`.
  - Redacted secret-like strings in three-body council error logs and moved Gemini API key from URL query to request header.
  - Redacted likely secrets in `scripts/injection_scan.sh` before Telegram reporting and skip-sent if Telegram placeholders are unset.
  - Re-ran preflight and validation suites; all passed.

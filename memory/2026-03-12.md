# 2026-03-12

- Added `scripts/sync-clawdia-skills.sh` to enforce repository-as-source-of-truth skill sync into `~/.openclaw/skills`.
- Script supports:
  - `--check` drift detection (non-zero when runtime differs from repo)
  - optional skill scoping via repeated `--skill`
  - automatic `openclaw-gateway.service` restart after sync (default behavior)
- Excluded runtime-only artifacts from sync (`targets/`, `_tg_offset`, `__pycache__`, `*.pyc`).
- Updated `.gitignore` to ignore Python artifacts and AutoImprove runtime outputs.
- Updated AutoImprove README with the sync workflow:
  - `./scripts/sync-clawdia-skills.sh --check`
  - `./scripts/sync-clawdia-skills.sh`
- Ran the sync script against runtime and verified:
  - no drift after deployment
  - gateway restarted cleanly
  - runtime autoimprove entrypoint executes without errors
  - Telegram config resolves valid bot token and active chat ID.

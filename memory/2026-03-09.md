# 2026-03-09

- Fixed live OpenClaw Telegram gateway runtime under `/home/openclaw/.openclaw`.
- Added `TELEGRAM_BOT_TOKEN` environment wiring to the user systemd unit for `openclaw-gateway.service`.
- Kept Telegram group mode on `allowlist` and set `groupAllowFrom: ["*"]` (global + account) with `groups["*"].requireMention: true` so group replies are mention-gated instead of silently dropped.
- Restarted `openclaw-gateway.service` and verified clean startup logs with no Telegram 404 errors and no group allowlist doctor warnings after the latest restart.
- Audited `clawdia-hertz-openclaw` runtime trace flow, removed legacy buildplan docs and obsolete `tests/` + `scripts/tests/` trees, then re-validated orchestrator trace behavior with post-cleanup smoke checks.
- Implemented self-improvement loop foundations for GatorBots helpdesk: added `coach_evaluator` scoring worker, dossier outcome labels + telemetry schema, automatic parent failure labeling on follow-up failures, nightly digest/regression backlog tooling, and end-to-end runtime validation script.
- Upgraded PatternScout to weighted curated retrieval with high-quality FRC repo registry, diversity caps, source receipts, freshness badges, learned pattern-card lane, and automatic weight/card learning scripts tied to labeled outcomes.
- Verified live `/home/openclaw/.openclaw/openclaw.json` uses a stricter schema than repo template docs; adding top-level `patternScout`/`selfImprovement` makes gateway config invalid, so reverted to backup and confirmed clean running service.
- Removed `docs/runtime/openclaw.template.json` to prevent config drift; updated docs references to `~/.openclaw/openclaw.json`, rebuilt `docs/repo_index.md`, and revalidated Telegram group runtime settings (`allowlist`, `groupAllowFrom: ["*"]`, `requireMention: true`) on the live config.
- Improved Telegram helpdesk continuity for group chats by making parent linkage conversation-aware (`chat_id + thread_or_topic_id`) with cross-session `parent_request_id` lookup, then added regression coverage in `scripts/validate-helpdesk-runtime.js`.
- Implemented full PatternScout hardening/speed pass: persistent per-root token index, snapshot-aware cache invalidation, lane-budgeted retrieval, quality-gate rejection for weak evidence, GitHub fallback circuit breaker, stronger snippet-level dedupe, weight-learning safeguards, metrics logging, and expanded fuzz/cache regression tests.
- Added a multi-process lane-execution pass for PatternScout via `scripts/patternscout-parallel-lanes.js` + `scripts/patternscout-parallel-lane-worker.js`, with automatic sequential fallback if process spawning is blocked and telemetry tracking for parallel lane usage.

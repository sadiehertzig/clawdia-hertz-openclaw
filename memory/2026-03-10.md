# 2026-03-10

- Investigated Clawdia outage after service/config refresh.
- Confirmed gateway process was up but Telegram channel was crash-looping with repeated `404 Not Found` on `deleteWebhook`, `deleteMyCommands`, and `setMyCommands`.
- Confirmed model runtime misconfiguration (`openai/gpt-5.4` without API key) and fixed live config to `openai-codex/gpt-5.4` for OAuth compatibility.
- Verified historical Telegram tokens from backup config against Telegram API (without exposing secrets); identified valid default token for `@clawdia_hertz_bot`.
- Applied verified token into live `/home/openclaw/.openclaw/openclaw.json` for both:
  - `channels.telegram.botToken`
  - `channels.telegram.accounts.default.botToken`
- Restarted `openclaw-gateway.service` and re-validated logs.
- Final state: gateway active/running, Telegram provider starts as `@clawdia_hertz_bot`, no new Telegram 404 crash-loop entries after restart.
- Noted remaining non-blocking warning: shell/CLI still warns about unresolved `OPENCLAW_GATEWAY_TOKEN` env in config context, but service runtime is operational for Telegram responses.

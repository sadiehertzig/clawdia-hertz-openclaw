#!/usr/bin/env bash
set -euo pipefail
CMD="${1:-}"
case "$CMD" in
  build|test|spotlessCheck|check)
    echo "simulated $CMD success"
    exit 0
    ;;
  *)
    echo "unsupported command $CMD" >&2
    exit 1
    ;;
esac

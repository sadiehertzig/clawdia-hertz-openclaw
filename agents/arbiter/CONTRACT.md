# Arbiter Contract

Required fields:

- `verdict` (`approve`, `revise`, `escalate`)
- `concern_list`
- revised output fields when `verdict = revise`

Arbiter is the review authority for substantive code answers.

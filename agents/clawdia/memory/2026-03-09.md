
## Config & System Hardening (Evening Session)

- **Memory Search Upgrades:** Enabled `hybrid` search (BM25 + Vector) with `MMR` diversity and `temporalDecay` recency weighting.
- **Failover & Reliability:** 
    - Added `local` fallback (node-llama-cpp) for memory embeddings to bypass Gemini rate limits.
    - Set global model default to `anthropic/claude-sonnet-4-6` with a fallback chain (Gemini Flash -> Codex).
    - Configured a dedicated fallback chain for the `deepdebug` agent to prevent "dead stops" on Opus rate limits.
- **Knowledge Base Expansion:**
    - Fixed search scope by adding `extraPaths` to `openclaw.json` pointing to the repo-root `memory/` directory.
    - Rewrote stub FRC documentation with deep-dive technical details for **REVLib (SPARK MAX)**, **Phoenix 6 (Talon FX)**, **PathPlanner**, and **WPILib Command-Based** architecture.
- **Pending Actions:**
    - **Wed March 11, 22:40 UTC:** Remind Gregory to get an OpenAI API key for faster embedding fallback. (Tracked in `HEARTBEAT.md` and `MEMORY.md`).
